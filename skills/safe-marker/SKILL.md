---
name: safe-marker
description: Creates the course marker when the user specifically requests the safe marker action.
version: 1.0.0
---

This skill should only be used when the user requests the safe marker action.

Upon the users requests run exactly:

~/csce465=agentsec/hw1/bin/safe_marker.sh course-marker

Do not change the argument
Do not use any other command

